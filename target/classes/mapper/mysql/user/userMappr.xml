<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lz.dao.UserDao">
	
	<resultMap id="BaseResultMap" type="com.lz.model.User" >
	    <id column="id" property="id" jdbcType="VARCHAR" />
	    <result column="user_name" property="userName" jdbcType="VARCHAR" />
	    <result column="password" property="password" jdbcType="VARCHAR" />
	    <result column="age" property="age" jdbcType="INTEGER" />
	</resultMap>
	
	<sql id="Base_Column_List" >
    	id, user_name, password, age
 	</sql>
	
	<!-- 根据id查找用户 -->
	<select id="findUserById" parameterType="java.lang.String" resultMap="BaseResultMap">
		select 
			<include refid="Base_Column_List" />
		from 
			user 
		where id = #{id,jdbcType=VARCHAR}
	</select>
	
	<!-- 添加数据 -->
	<insert id="insertUser" parameterType="com.lz.model.User">
		insert into user(id,user_name,password,age) values(#{id},#{userName},#{password},#{age})
	</insert>
	
	
</mapper>